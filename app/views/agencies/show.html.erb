<h1><%= @agency.portfolio.name%> - <%= @agency.name %></h1>
<ul>
	<% @agency.programs.each do |program| %>
		<li> <%= link_to program.name, [@agency.portfolio, @agency,program] %> </li>
	<% end %>
<ul>
